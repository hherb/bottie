# Bottie handover

Last verified: 2026-09-14

## Start here

PR #167 merged into `main` at `bd07efb`. The current branch completes Milestone 8.2 generated-image recovery.
Read Milestone 8.3 in `ROADMAP.md`, then the existing private worker boundaries in `src-tauri/src/python_execution.rs`
and `python-runner/` before defining a separate image-worker protocol.

## Completed slice

- Portable conversation export version 6 includes path-free generated-image metadata and selected-lineage PNGs under
  `generated-images/<sha256>.png`. It deduplicates shared bytes and verifies exact size/hash at write time, failing
  without a partial destination when bytes are missing or changed.
- Portable backup version 2 embeds every completed generated PNG needed by the database, validates exact content, and
  restores attachment and generated-image roots with rollback. Version 1 attachment-only backups remain restorable;
  corruption recovery preserves displaced generated bytes beside the damaged database.
- Restart converts abandoned pending image responses to stable `interrupted` failures that retain their exact durable
  retry request. Pending image work blocks conflicting send, branch, archive, delete, forget, and memory-policy changes;
  automatic retention skips it.
- Garbage collection retains completed PNGs referenced by any branch or Trash conversation, removes old orphaned PNGs
  and interrupted temporary files, and reclaims a blob only after its final durable reference is forgotten.
- Schema-24 rewind fixtures now remove both generated-image tables, and the newer-schema rollback test derives its
  version from `CURRENT_SCHEMA_VERSION` instead of a stale literal.

## Validation

- `npm run format:check`, `npm run check`, `npm test`, and `npm run build` pass. Frontend tests: 360 passed, 3 skipped;
  Svelte diagnostics: 0 errors and 0 warnings.
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` and `cargo check --manifest-path
  src-tauri/Cargo.toml` pass. The standard target directory was held by a pre-existing Cargo build lock, so the check
  and serialized test run used isolated `/private/tmp/bottie-generated-recovery-target`.
- The host-local serialized Rust suite passes: 537 passed, 36 intentionally ignored, plus 1 updater-evidence test.
  This includes the loopback image-download tests. `git diff --check` and final self-review pass.
- No WebView presentation changed, so browser/native UI review was not applicable. No live or billable Model Studio
  generation was run.

## Next slice

Begin Milestone 8.3 with the protocol contract only. Add pure Rust, versioned, closed-schema envelopes for
`hello/capabilities`, `load`, `generate`, `progress`, `cancel`, `result`, and `shutdown`, with named maximum frame and
field limits and length-delimited private-pipe framing. Test valid round trips plus unknown fields/messages, unsupported
versions, malformed/truncated/oversized frames, duplicate terminal results, and sensitive/path-shaped error rejection.

Do not launch a process, open localhost, acquire models, probe hardware, load a runtime, generate an image, add UI or
provider fallback, or reuse/broaden the user-approved Python tool runtime in that slice. Keep exact hosted
`qwen-image-2.0-2026-03-03` separate from local `Qwen/Qwen-Image-2512`.

Do not merge, dispatch workflows, sign, release, publish, distribute, or perform Store work without separate
authorization. Preserve unrelated untracked logo-kit, screenshot, and Linux public-key files.
